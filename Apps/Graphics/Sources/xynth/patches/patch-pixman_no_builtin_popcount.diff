--- xynth-0.8.90.orig/src/lib/pixman/icint.h	2007-09-25 10:38:19.000000000 +0200
+++ xynth-0.8.90/src/lib/pixman/icint.h	2009-02-20 02:42:51.000000000 +0100
@@ -803,7 +803,7 @@
    implemented with a single instruction.  There is a fallback definition
    in libgcc in case a target does not have one, which should be just as
    good as the static function below.  */
-#if __GNUC__ > 3 || (__GNUC__ == 3 && __GNUC_MINOR__ >= 4)
+#if 0 /* __GNUC__ > 3 || (__GNUC__ == 3 && __GNUC_MINOR__ >= 4) */
 static INLINE int
 _FbOnes(unsigned int mask)
 {
